# T<NN> — <краткое имя задачи>

<!-- Resource-Lock: vnc_1c   (раскомментируй для singleton-ресурсов: 1С / VNC :99 / SOCKS / прод-деплой) -->
<!-- No-Respawn: true        (раскомментируй для LIVE/1С-тасков: агент дёргает живую кассу/1С/фискалку.
     Тогда MAX_RESPAWN=0 и оркестратор НЕ перезапускает таск после смерти/AMBIGUOUS — уходит в карантин
     и пингует человека, а не гоняет агента по кругу (шторм рестартов кассы). Авто-включается также, если
     Resource-Lock ∈ {node1c,node,1c,vnc_1c,live,prod,fiscal,ofd}. Для таких тасков делай шаги идемпотентными:
     перед разрушающим действием (рестарт киоска / переоткрытие .epf) проверь пробой, что оно ещё нужно.) -->

## Цель
<Одно-два предложения: что должно быть сделано и зачем.>

## Контекст / что прочитать
- <спеки, charter, предыдущий report, файлы>

## Что сделать
<!-- 🔴 Таск-ГИПОТЕЗА, а не таск-ВЫВОД (GOTCHAS #8): если причина/диагноз ещё НЕ доказан — формулируй его как
     гипотезу для ПРОВЕРКИ, а не как факт, и дай контр-гипотезу («может, чинить нечего / артефакт теста»).
     Фазу-ФИКС делай строго условной: «выполнять ТОЛЬКО ЕСЛИ гейт проверки провалился на ИДЕНТИЧНОМ входе».
     Обязателен явный выход-СТОП: «нет расхождения/бага → STATUS: SUCCESS с выводом ‘чинить нечего’, код НЕ трогать,
     не пересобирать, не деплоить». Иначе агент по инерции «улучшит» рабочий код. -->
1. <шаг>
2. <шаг>

## Жёсткие гейты приёмки (числами, проверяемо)
- [ ] <критерий 1>
- [ ] <критерий 2>

## Что НЕ трогать
- <измерители, чужие файлы, прод-состояние, фискалка/ОФД/маркировка без явного разрешения>

## Отчёт (обязательно)
Записать `reports/report_T<NN>.md`. ПОСЛЕДНЯЯ строка отчёта — ровно одна из:

```
STATUS: SUCCESS
STATUS: FAIL
STATUS: BLOCKED
STATUS: PARTIAL
```

SUCCESS — только с доказательством верификации (не «файл создан», а «проверено, работает»).
